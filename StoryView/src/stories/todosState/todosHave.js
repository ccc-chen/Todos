// Generated by CoffeeScript 2.1.1
var CFX;

import {
  storiesOf
} from '@storybook/react';

import {
  prefixDom
} from 'cfx.dom';

import ListHave from '../../components/Todos/list';

CFX = prefixDom({ListHave});

export default function() {
  return storiesOf('TodosState', module).add('Have', () => {
    var c_ListHave;
    ({c_ListHave} = CFX);
    return c_ListHave({
      data: [
        {
          objectId: 0,
          todo: '完成',
          isCompleted: true
        },
        {
          objectId: 1,
          todo: '未完成',
          isCompleted: false
        }
      ],
      styleChange: (function(objectId, isCompleted) {
        if (isCompleted === true) {
          return {
            textDecorationLine: 'line-through',
            opacity: 0.4
          };
        }
      }).bind(this),
      hasClick: function(str) {
        console.log('key:');
        return console.log(str);
      }
    });
  });
};
